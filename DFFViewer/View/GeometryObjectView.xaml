<UserControl x:Class="Heavy.DFFViewer.View.GeometryObjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="100"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <DockPanel LastChildFill="False">
      <Slider DockPanel.Dock="Top" x:Name="cameraWidthSlider" Minimum="0" Maximum="20"/>
    </DockPanel>
    <Viewport3D Grid.Column="1" ClipToBounds="True">
      <Viewport3D.Camera>
        <OrthographicCamera Width="{Binding ElementName=cameraWidthSlider, Path=Value}" LookDirection="5,0,0"/>
      </Viewport3D.Camera>
      <Viewport3D.Children>
        <ModelVisual3D>
          <ModelVisual3D.Content>
            <AmbientLight/>
          </ModelVisual3D.Content>
        </ModelVisual3D>
        <ModelVisual3D Content="{Binding ModelGroup}" />
      </Viewport3D.Children>
    </Viewport3D>
  </Grid>
</UserControl>
