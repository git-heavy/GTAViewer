<Window x:Class="Heavy.DFFViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Heavy.DFFViewer.ViewModels"
        xmlns:v="clr-namespace:Heavy.DFFViewer.Views"
        Title="MainWindow" Height="350" Width="525">
  <Window.CommandBindings>
    <CommandBinding Command="ApplicationCommands.Open" Executed="OpenCommandExecuted" CanExecute="CommandBinding_CanExecute" />
  </Window.CommandBindings>  
  <StackPanel>
    <!--<Menu IsMainMenu="True">
      <MenuItem Header="_File">
        <MenuItem Command="ApplicationCommands.Open" />
      </MenuItem>
    </Menu>-->
    <!--<my:ModelViewPort x:Name="modelViewPort" />-->
    <v:CameraView DataContext="{Binding CamViewModel}" />
    <Button Click="Button_Click">TEST</Button>
  </StackPanel>
</Window>
